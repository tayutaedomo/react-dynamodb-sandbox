# Antigravity Agent Rules

## 1. コミットに関する厳格なルール (Strict Rule for Commits)
**絶対に勝手に `git commit` を実行しないでください。**
いかなる場合でも、コードの変更後はユーザーに確認を求め、ユーザーが自身で動作確認を行った後、明示的に「コミットしてください」と指示した場合のみコミットを実行してください。

## 2. テスト駆動と品質保証 (Testing and Quality Assurance)
**新しいAPIエンドポイントやビジネスロジックを追加した際は、必ず単体テスト (Unit Tests) を作成・追加してください。**
テストのないロジックの追加は許可されません。また、テストはインフラに依存しない形（モックを利用するなど）で作成し、自動テストとして高速に実行できるようにしてください。

## 3. コードフォーマットと静的解析 (Linting and Formatting)
**Python コードを変更した後は、必ず `ruff` を使用して Linter (静的解析) と Formatter (整形) を実行してください。**
ユーザーへコードを提示する前に、`uv run ruff check --fix` および `uv run ruff format` を実行して、コードの品質とスタイルが一貫していることを担保してください。

## 4. アーキテクチャ決定記録 (ADR) の提案義務 (Mandatory ADR Drafting)
**重要な技術的決定、設計のトレードオフ、または採用を見送ったアプローチがあった場合、Agent は自発的に ADR の草案を作成しなければなりません。**
Agent はユーザーからの指示を待つのではなく、作業の区切りごとに「この決定は ADR に残すべきか？」を必ず自問自答してください。必要と判断した場合は草案をユーザーに提示し、ユーザーに採用可否の判断を仰いでください。
